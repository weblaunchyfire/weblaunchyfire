export const metadata = {
  title: "Launch Your Website | Web Launchy Fire",
  robots: {
    index: false,
    follow: false,
  },
};

export default function LaunchLayout({ children }) {
  return children;
}
